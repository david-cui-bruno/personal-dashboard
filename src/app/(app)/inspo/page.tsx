// The inspo board (#140, build brief docs/inspo.md). moodboard | people boards of
// images you paste/drag/upload; open one to add sticky notes (phase 1b).
import { InspoBoard } from "@/components/inspo/inspo-board";

export default function InspoPage() {
  return <InspoBoard />;
}
