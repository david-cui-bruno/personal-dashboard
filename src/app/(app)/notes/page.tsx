// Notes — placeholder. The reverse-chron stream (journals + freeform notes) and
// the editor are the Phase 1 "Notes" slice (see docs/roadmap.md).
export default function Notes() {
  return (
    <div className="mx-auto max-w-[700px] px-10 py-14">
      <h1 className="text-[33px] font-black lowercase tracking-tight">notes</h1>
      <p className="mt-10 text-[15px] font-bold lowercase text-ink-2">
        the notes stream + editor land in phase 1.
      </p>
    </div>
  );
}
