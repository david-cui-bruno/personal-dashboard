// Notes (spec §6): the reverse-chronological stream of journals + freeform notes.
import { NotesStream } from "@/components/notes/notes-stream";

export default function NotesPage() {
  return <NotesStream />;
}
